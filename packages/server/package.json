{
  "name": "@artifacts/server",
  "type": "module",
  "scripts": {
    "dev": "cross-env NODE_ENV=development tsx watch --clear-screen=false --env-file=../../.env src/index.ts",
    "prod": "cross-env NODE_ENV=production tsx --env-file=../../.env src/index.ts",
    "build": "tsc -p ./tsconfig.build.json",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@artifacts/shared": "file:../shared",
    "@fastify/cors": "^10.0.1",
    "@fastify/static": "^8.0.2",
    "fastify": "^5.1.0",
    "openapi-fetch": "^0.13.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "tsx": "^4.19.2"
  }
}
